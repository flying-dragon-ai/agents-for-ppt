实现 pptm-domain crate 的核心类型：

参考文件：
- tools/project_utils.py（CANVAS_FORMATS 定义）
- .claude/team-plan/tauri-rust-rewrite.md（Task 1 的代码骨架）

任务：
1. 实现 src-tauri/crates/pptm-domain/src/config.rs：
   - 定义 CanvasFormat 结构体
   - 定义 CANVAS_FORMATS 常量（使用 lazy_static）
   - 实现 normalize_canvas_format 函数
   - 添加单元测试

2. 实现 src-tauri/crates/pptm-domain/src/model/project.rs：
   - 定义 ProjectInfo 结构体
   - 定义 ProjectMetadata 结构体
   - 添加 serde 序列化支持

3. 更新 src-tauri/crates/pptm-domain/src/lib.rs：
   - 导出 config 和 model 模块

4. 更新 src-tauri/crates/pptm-domain/Cargo.toml：
   - 添加必要的依赖：serde, lazy_static

要求：
- 代码必须能编译通过
- 包含完整的单元测试
- 使用中文注释
- 参考 Python 版本的实现逻辑