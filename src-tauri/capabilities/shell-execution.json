{
  "$schema": "https://tauri.app/v2/schemas/capability.schema.json",
  "identifier": "shell-execution",
  "description": "Capability for executing shell commands (Python tools)",
  "windows": ["main"],
  "permissions": [
    "shell:default",
    "shell:allow-execute",
    "shell:allow-spawn"
  ]
}
